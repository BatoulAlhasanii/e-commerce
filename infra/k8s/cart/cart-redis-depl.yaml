apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-redis-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-redis
  template:
    metadata:
      labels:
        app: cart-redis
    spec:
      containers:
        - name: cart-redis
          image: redis:7
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: cart-redis-data
              mountPath: /data
      volumes:
        - name: cart-redis-data
          persistentVolumeClaim:
            claimName: cart-redis-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: cart-redis-srv
spec:
  selector:
    app: cart-redis
  ports:
    - name: cart-db
      protocol: TCP
      port: 6379
      targetPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: cart-redis-node-port-srv
  labels:
    app: cart-redis
spec:
  type: NodePort
  selector:
    app: cart-redis
  ports:
    - name: cart-node-port-db
      protocol: TCP
      port: 6379
      targetPort: 6379
      nodePort: 30003
