apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-postgres-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-postgres
  template:
    metadata:
      labels:
        app: order-postgres
    spec:
      containers:
        - name: order-postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: order-postgres-data
              mountPath: /var/lib/postgresql/data
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: order-config
                  key: db-name
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: order-config
                  key: db-username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-secret
                  key: db-password
      volumes:
        - name: order-postgres-data
          persistentVolumeClaim:
            claimName: order-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: order-postgres-srv
spec:
  selector:
    app: order-postgres
  ports:
    - name: order-db
      protocol: TCP
      port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: order-postgres-node-port-srv
  labels:
    app: order-postgres
spec:
  type: NodePort
  selector:
    app: order-postgres
  ports:
    - name: order-node-port-db
      protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30004
